# Copie para `.env` e preencha. O `.env` está no .gitignore.
# Toda variável nova precisa ser declarada em `src/lib/env.ts`, senão o boot falha.

# development | test | production
NODE_ENV=development

# Variáveis expostas ao browser exigem o prefixo VITE_.
# VITE_API_URL=http://localhost:3000
